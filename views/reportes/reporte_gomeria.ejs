<%
const estadoNombre = (e) => e==1?'Nueva':e==2?'Usada':'Recapada';
const currentPage = 'reportes';
const body = `
<div class="list-head">
  <h2>Reporte por Gomería</h2>
</div>

<form method="get" action="/reportes/reporte_gomeria" class="list-filters">
  <strong>Gomería:</strong>
  <select name="gomeria">
    <option value="0">Seleccione...</option>
    ${gomerias.map(g=>`<option value="${g.id}" ${g.id==gomeria?'selected':''}>${g.nombre}</option>`).join('')}
  </select>
  <button type="submit" class="btn-filter">Ver</button>
</form>

${cubiertas.length > 0 ? `
<table>
  <thead><th>Fuego</th><th>Modelo</th><th>Medida</th><th>Estado</th><th>Km</th></thead>
  ${cubiertas.map((c,i)=>`<tr class="${i%2===0?'light':'dark'}">
    <td align="center">${c.fuego||'-'}</td>
    <td align="center">${c.marca||''} ${c.modelo_nombre||''}</td>
    <td align="center">${c.medida||'-'}</td>
    <td align="center">${estadoNombre(c.estado)}</td>
    <td align="center">${c.km||0}</td>
  </tr>`).join('')}
</table>` : gomeria > 0 ? '<p style="color:var(--text-muted); margin-top:10px;">Sin cubiertas para esta gomería.</p>' : ''}

<a href="/reportes" class="list-back">&laquo; Volver</a>
`;
%>
<%- include('../layout', { body, user, currentPage }) %>
