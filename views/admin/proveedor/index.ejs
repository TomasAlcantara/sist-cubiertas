<%
const currentPage = 'admin';
const body = `
<div class="list-head">
  <h2>Proveedores</h2>
  <a href="/admin/proveedor/nuevo" class="btn-nuevo">Nuevo Proveedor</a>
</div>
<table>
  <thead><th>Proveedor</th><th>Teléfono</th><th>Mail</th><th style="width:150px;">Editar</th></thead>
  ${proveedores.map((p,i)=>`<tr class="${i%2===0?'light':'dark'}">
    <td>${p.proveedor}</td>
    <td align="center">${p.tel||'-'}</td>
    <td align="center">${p.mail||'-'}</td>
    <td align="center"><a href="/admin/proveedor/editar?proveedor=${p.id}">EDITAR</a></td>
  </tr>`).join('')}
</table>
${totalPages > 1 ? `<div class="list-pagination">
  ${Array.from({length:totalPages},(_,i)=>i+1).map(p =>
    p==pagina ? `<strong class="pg-current">${p}</strong>` : `<a href='?pagina=${p}'>${p}</a>`
  ).join(' | ')}
</div>` : ''}
<a href="/admin" class="list-back">&laquo; Volver</a>
`;
%>
<%- include('../../layout', { body, user, currentPage }) %>
